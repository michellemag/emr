services:
  - type: web
    name: emr-api
    env: node
    plan: free
    buildCommand: npm ci --include=dev && npm run install-all && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_PATH
        value: ./emr.db
